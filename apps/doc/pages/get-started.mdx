import { Tab, Tabs } from 'nextra-theme-docs'

# Get Started

<Tabs items={['node.js', 'python']}>
  <Tab>
    ```js filename="getVenezuelaLocations.js" {4} copy
      const res = await fetch('https://locations-venezuela.com/api/locations')
      const data = await res.json()

      console.log(data) // <-- [ { states: { municipalities: { zones } } } ]
    ```
  </Tab>

  <Tab>
    ```py filename="getVenezuelaLocations.py" {6} copy
      import requests
        
      URL = 'https://locations-venezuela.com/api/locations'
      res = requests.get(url = URL, params = PARAMS)

      data = res.json() # <-- [ { states: { municipalities: { zones } } } ]
    ```
  </Tab>
</Tabs>
